version: '3'

services:
  yourls:
    image: yourls
    restart: always
    ports:
      - '80:80'
      - '443:443'
    environment:
      YOURLS_DB_PASS: password
      YOURLS_SITE: http://YOURLS_SITE
      YOURLS_USER: admin
      YOURLS_PASS: PASSWORD

  mysql:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: PASSWORD
    volumes:
      - ./mysql:/var/lib/mysql
